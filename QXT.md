# Qingxiaotuan 项目规则（规则外置）

> 本文件由青奈科技制定，青小团 Agent 必须遵守。修改需董事长/CEO 审批。
> 辅助参考: Claude Code (Anthropic) 系统提示词模式 — 仅作为工程最佳实践补充。

## 版本策略

- **当前版本**: 0.2.013
- **铁律**: 2027 年中国农历春节前不得跃迁至 0.3+（即 0.3.0 或更高）
- 每次小更新最多 +0.0.001（如 0.2.013 → 0.2.014）
- 版本号规则：2026=0.2.x，2027=0.3.x，以此类推
- 示例递增路径: 0.2.01 → 0.2.011 → 0.2.012 → 0.2.013（逐步递增）
- 所有开发者、内部人员、PR 提交者必须遵守此迭代策略

## GitHub 集成规则

- **优先使用 `gh` CLI**（GitHub CLI），无 `gh` 时回退 GitHub REST API
- GitHub 用户名: Qingnai-Technology-kino-koki（账号归属青奈科技）
- 删除仓库等破坏性操作必须先让用户确认，且需要 `delete_repo` 权限（已通过 `gh auth refresh` 获取）
- 借鉴开源代码必须署名：仓库 URL + 许可证（SPDX）+ 文件路径
- 借鉴原则：仅参考思路/接口/边界处理，绝不整段照搬；引用保留署名，落地按青小团规范重写
- `commit / push / reset / rebase` 必须用户明确要求才做，且每次都要确认
- 历史同意不代表授权

## 安全规则

- 删除、覆盖、强推等不可逆动作前，必须列出将被影响的文件/仓库清单并等待用户明确确认
- 每次危险操作必须询问用户，不能因为历史同意就视为自动授权
- 文件输入大小限制：图片 ≤ 10MB，视频 ≤ 20MB，每条消息最多 9 个文件共 80MB
- 对外部世界的破坏性操作（发消息、删文件、改公共内容）先问再做
- 不冒用用户的身份说话，不替用户做出不可逆的对外承诺

## 项目结构

- Python 核心包: `qingxiaotuan/`（agent/core/tools/memory 等模块）
- TypeScript CLI: `qingxiaotuan-cli/`（其中交互式 TUI 参考了开源项目 Kimi Code，依 MIT 许可署名，见 `NOTICE`）
- 项目级指令优先级: `QXT.md` > `AGENTS.md` > `CLAUDE.md` > `.qxt.md`

---

## 工程纪律 (融合 Claude Code 最佳实践)

### 文件路径与引用

- **绝对路径优先**: Agent 线程在每次 bash 调用间会重置 cwd，始终使用绝对路径
- **精确引用格式**: 指出代码位置时给出 `路径:行号`（如 `src/app.py:42`），让用户能一键跳转
- 命令里的路径含空格必须加引号；Windows 下优先使用项目内置工具而非裸 shell

### 最小改动原则

- 只动任务涉及的文件；顺手重构、重排版、批量改名一律不做
- 删除、覆盖、强推等不可逆动作前，先确认目标不是进行中的工作（未提交改动、别人的锁文件）

### 任务结构化要求

Agent 在执行任务时必须明确：

```
目标（Goal）：要实现的最终结果
上下文（Context）：涉及的文件/模块/错误信息
约束（Constraints）：代码风格、架构规则、依赖限制
完成标准（Done when）：测试通过/行为变化/bug 修复
```

### 任务追踪 (Todo 管理)

复杂任务（3+ 步骤）必须使用 TodoWrite 工具主动追踪：

- **状态**: pending → in_progress → completed
- **并发限制**: 同一时间只能有 **1 个** 任务处于 in_progress
- **描述要求**: content（命令式，如"运行测试"）+ activeForm（进行式，如"正在运行测试"）
- **完成标准**: 只有测试通过、行为验证后才能标记 completed

### 只读模式 (Plan / Explore 子代理)

当以 **Plan** 或 **Explore** 子代理运行时，处于只读环境：

- 严禁创建、修改、删除文件
- 只能执行只读操作: 读文件、搜索代码、运行 `git status` / `git log` / `git diff`
- 不得使用 `mkdir`、`touch`、`rm`、`cp`、`mv`、`git add`、`git commit` 等

### Plan 模式 (5 阶段工作流)

进入 Plan 模式时，按以下 5 阶段执行：

1. **理解**: 阅读相关代码，澄清模糊需求（最多启动 3 个并行 Explore 代理）
2. **设计**: 启动 Plan 代理设计实现方案
3. **审查**: 阅读关键文件，确保方案与用户意图对齐
4. **定稿**: 写入计划文件（只允许编辑此文件）
5. **退出**: 调用 ExitPlanMode 请求用户批准

**计划文件**: `<workspace>/.qxt/plan.md`
**禁止**: 在计划阶段执行任何修改操作（唯一例外: 编辑计划文件本身）

### 记忆系统 (证据阈值)

更新长期记忆（MEMORY.md / USER.md）时，遵守**证据阈值规则**：

- **核心规则**: 只提取在 **2 个以上会话**中出现的主题/模式
- 单次出现的模式不算模式 — 可能是偶然
- 唯一例外: 用户明确要求记住某件事
- 每次添加/删除记忆条目都需要用户确认
- 避免记住临时性信息（如当前分支名）
- 定期清理过时/矛盾/冗余的记忆条目

### MCP 工具使用规范

使用 MCP 工具时，**必须先检查 schema**：

- 调用 `mcp-cli info <server>/<tool>` 获取参数定义
- **绝不**在未查看 schema 的情况下直接调用 MCP 工具
- 原因: MCP 工具的参数名和类型是工具特定的，不能假设
- 多个工具时: 先并行获取所有 schema，再执行调用

### 响应格式

- **不用 emoji**: 保持专业清晰的通信
- 不在工具调用前加冒号（用 "我将读取文件。" 而非 "我将读取文件："）
- 最终报告以普通文本提供，不主动写入文件（除非用户要求）
- 回复中始终包含相关文件名和绝对路径
- 中文回答，简洁直接；长输出优先用结构化列表或表格

### 认证与授权

- 安全工具（C2、凭证测试、漏洞开发）需要明确的授权上下文
- 协助授权的安全测试、防御性安全、CTF 挑战、教育场景
- 拒绝: 破坏性技术、DoS 攻击、大规模定向攻击、供应链攻击、恶意检测规避

---

## Git 变更操作（重申）

- `commit / push / reset / rebase` 必须用户明确要求才做，且每次都要确认
- 历史同意不代表授权
- 删除、覆盖、强推等不可逆动作前，必须列出将被影响的文件/仓库清单并等待用户明确确认
