# 青小团 CLI（Qingxiaotuan Agent CLI）

## 项目简介

青小团 CLI 是一个面向开发者、研究者与自动化工作流的独立 Agent 命令行工具。项目致力于把大语言模型的理解、规划、执行与反思能力带到终端中，让用户可以通过自然语言完成代码阅读、项目修改、命令执行、资料整理和任务编排。

项目参考 DeepSeek Harness 与 Hermes Agent 的设计理念，在保持轻量、透明和可扩展的同时，针对中文使用场景、个人开发流程以及本地项目协作进行了重新设计。青小团 CLI 不只是一个聊天窗口，而是一个能够理解上下文、调用工具、持续推进任务并向用户汇报结果的工程化助手。

## 设计目标

1. **自然交互**：用户使用自然语言描述目标，CLI 负责拆分步骤并给出清晰反馈。
2. **安全执行**：涉及文件修改、命令运行和外部访问时，优先展示计划并请求必要确认。
3. **本地优先**：项目上下文、配置和执行记录尽可能保存在用户可控的环境中。
4. **模型无关**：通过统一接口适配不同厂商、不同规模和不同部署方式的模型。
5. **工具可插拔**：文件系统、终端、搜索、版本控制和自定义工具均可独立扩展。
6. **结果可验证**：Agent 不仅生成答案，还应检查变更、运行测试并说明验证结果。
7. **开发者友好**：命令简洁，日志可读，错误可追踪，配置易理解。

## 核心理念

青小团 CLI 将一次任务看作一个可观察、可暂停、可恢复的执行过程。用户提出目标后，系统先分析需求，再建立任务计划；每个步骤都可以调用合适的工具，工具返回结果后由模型继续判断下一步；任务结束时，系统总结已完成的工作、未完成的事项、产生的文件变化以及可能的风险。

这种循环并不追求让模型无条件地执行更多动作，而是强调在正确的边界内完成可靠的工作。Agent 应当知道自己能够做什么、不能做什么，也应当在信息不足时明确说明，而不是凭空编造结果。

## 主要能力

### 项目理解

青小团 CLI 可以读取项目目录结构，识别常见语言、框架、配置文件和文档，并建立与当前任务相关的上下文。它会尽量减少无关内容的读取，降低上下文消耗，同时保留足够的信息来理解模块之间的关系。

### 代码协作

用户可以要求 Agent 解释代码、定位问题、设计接口、重构模块、补充测试、生成文档或审查变更。对于修改类任务，系统应优先展示影响范围，并将修改限制在用户指定的文件和区域内。

### 终端操作

在获得授权后，Agent 可以执行构建、测试、格式化、静态检查和其他开发命令。命令执行应记录输入、输出、退出状态和耗时，方便用户判断结果是否可信。

### 任务规划

复杂任务可以拆分为多个阶段，例如理解现状、制定方案、实施变更、运行验证和整理报告。每个阶段都有明确目标，失败时可以回退、重试或交给用户决策。

### 上下文管理

系统会根据任务需要选择文件、摘要历史信息，并控制发送给模型的内容规模。上下文管理既关注模型效果，也关注隐私、成本和响应速度。

### 多模型接入

项目通过统一的模型适配层隐藏不同服务的调用差异，使上层 Agent 能够使用一致的消息、工具调用和流式输出接口。用户可以按照任务类型选择速度更快、推理更强或部署位置不同的模型。

## 工作流程

一次典型任务通常包含以下阶段：

1. 接收用户目标。
2. 读取必要的项目上下文。
3. 识别约束、风险和验收标准。
4. 生成可执行的任务计划。
5. 在需要时请求用户确认。
6. 调用文件、终端或其他工具。
7. 读取工具结果并更新任务状态。
8. 对结果进行检查和验证。
9. 汇总变更、测试结果和后续建议。

Agent 可以在这些阶段之间循环，但不应隐藏关键行为。透明的计划和日志能够帮助用户理解系统正在做什么，也便于在错误发生时快速定位原因。

## 安全原则

青小团 CLI 将安全作为默认能力，而不是事后补救。系统应区分只读操作与写入操作，区分低风险命令与高风险命令，并在删除文件、修改大量内容、访问敏感目录、上传数据或执行不可逆操作前明确提示。

安全策略可以按项目、用户和会话进行配置。用户可以允许常用的低风险操作，也可以对特定命令设置禁止规则。任何自动化能力都不应削弱用户对本地环境的控制权。

## 可扩展架构

项目可以划分为交互层、Agent 编排层、模型适配层、上下文层、工具层、策略层和存储层。各层通过稳定接口协作，避免把具体模型、终端实现或界面逻辑耦合在一起。

- **交互层**负责命令解析、输入输出、流式显示和用户确认。
- **编排层**负责计划、循环、状态管理和错误恢复。
- **模型层**负责请求构造、响应解析、重试和供应商适配。
- **上下文层**负责文件选择、摘要、缓存和历史管理。
- **工具层**负责文件、终端、搜索、Git 和第三方扩展。
- **策略层**负责权限、沙箱、审计和风险控制。
- **存储层**负责配置、会话、日志和可恢复状态。

## 中文体验

青小团 CLI 优先考虑中文用户的表达习惯。用户可以使用自然、简短甚至不完整的中文描述目标，系统应通过追问、推断和检查逐步补齐任务信息。输出内容强调结论先行、步骤清晰、术语统一，并在必要时保留英文命令、路径和错误信息。

## 开源与二次开发

项目吸收 DeepSeek Harness 与 Hermes Agent 的优秀思路，但不以简单复制为目标。相关源码包可以从各自的 GitHub 仓库获取并进行学习，青小团 CLI 则在此基础上围绕独立架构、中文交互、本地安全和开发工作流进行二次设计。

二次开发者可以替换模型适配器、增加工具、调整提示词、实现新的界面或接入企业内部服务。扩展应尽量遵守清晰的接口约定，并在文档中说明权限、依赖和失败处理方式。

## 使用场景

- 阅读陌生项目并生成结构说明。
- 根据需求修改代码并补充测试。
- 分析构建失败、运行异常和日志信息。
- 执行重复性的本地开发命令。
- 生成接口文档、变更说明和发布记录。
- 辅助整理会议材料、技术方案和项目任务。
- 将多个命令组合为可复用的自动化流程。
- 作为团队内部知识库和工程助手的终端入口。

## 配置与会话

配置文件用于保存模型地址、认证方式、默认权限、输出风格和工具开关。敏感信息不应直接写入项目仓库，推荐使用环境变量、系统密钥链或其他安全凭据存储方式。

会话可以保存任务目标、计划、工具调用和最终报告。恢复会话时，系统应提示上下文是否过期，并重新验证关键文件状态，避免基于已经改变的环境继续执行旧计划。

## 可观测性

可靠的 Agent 需要能够解释自己的行为。青小团 CLI 应提供适度的调试日志、工具调用记录、模型响应状态、重试原因和验证结果。普通模式保持简洁，调试模式提供足够的细节，但不应在日志中泄露密钥和不必要的敏感内容。

## 质量保障

项目应通过单元测试、集成测试、端到端测试和真实项目试用持续改进。重点覆盖模型异常、工具失败、网络中断、上下文过长、权限拒绝、文件冲突和用户取消等情况。

每次变更都应尽可能具备明确的验收方式。对于代码修改，至少应进行语法检查或相关测试；对于文档和配置修改，应检查格式、路径、引用和兼容性。

## 发展方向

未来可以继续完善图形化界面、远程 Agent、团队协作、插件市场、长期记忆、知识检索、任务队列和更细粒度的沙箱能力。同时应持续关注成本控制、隐私保护、提示注入防护、供应链安全和模型行为评估。

## 项目愿景

青小团 CLI 希望成为一个可信、易用、开放且真正属于用户的 Agent 工具。它不替用户做出所有决定，而是帮助用户更快理解问题、更稳妥地执行计划、更清楚地验证结果。无论任务规模大小，系统都应尊重用户意图，保持操作透明，并让每一次自动化都能够被理解、被检查和被掌控。

青小团 CLI（Qingxiaotuan Agent CLI）将持续以工程实践为基础，连接模型能力与真实工作流，为个人开发者、开源项目和团队协作提供可靠的终端智能。
