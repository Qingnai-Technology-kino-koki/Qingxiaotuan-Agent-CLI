# 青小团 · Qingxiaotuan Agent CLI

> **«Model + Harness = Agent»** — разделяем «как думать» и «как выполнить это на безопасно», и отдаём тебе оба ключа.
> Безопасный по умолчанию, модельно-нейтральный, чисто на Python харнесс ИИ-агента. `v0.2.014` · MIT · Python ≥ 3.10

**Язык / Language:** [English](README.md) · [简体中文](README_zh-CN.md) · [繁體中文](README_zh-TW.md) · [日本語](README_ja.md) · [한국어](README_ko.md) · [Español](README_es.md) · [Português (Brasil)](README_pt-BR.md) · [Français](README_fr.md) · [Deutsch](README_de.md) · **Русский**

**Перед тем как закатывать рукава:** [SECURITY.md](SECURITY.md) (модель угроз) · [CHANGELOG.md](CHANGELOG.md) (дисциплина версий) · [ARCHITECTURE.md](ARCHITECTURE.md) (архитектура) · `qxt models list-providers`

---

## В двух словах

Большинство агент-CLI рождаются *обвенчанными с одним провайдером*. Qingxiaotuan — наоборот: **шасси без прописанного мозга**. Хот-свап между 48 провайдерами, полностью офлайн, откат фатальных косяков и радиус поражения **до** того, как команда выстрелит. **Модель думает — он держит базу.**

**Чем это НЕ является:** не ребёнок какой-то модели (хот-свап, self-host, офлайн — как захочешь); не придаток IDE (стандартный терминальный инструмент, может управляться через ACP/IDE); не матрёшка (микроядро и плагины — для тех, кто любит крутить; одна `setup` — для тех, кто просто хочет работать).

---

## Чем он отличается

| Что даёт | Насколько топ |
|---|---|
| 🛡️ **Сначала безопасность** | Четверо ворот: статическая оценка, пред-блокировка по радиусу поражения, красная линия YOLO на подстраховке, транзакционный журнал `/undo` для точного отката |
| 🔌 **Модельно-нейтральность** | 48 провайдеров + локальный Ollama + хот-свап на лету + авто-маршрутизация (`router.*`) |
| 🧠 **Три главных цикла** | ReAct / Planner-Execute / DevLoop — подключай любой, "одно ядро, разные ритмы мышления" |
| 🔧 **Микроядро** | `@plugin` в одну строку, реестр сервисов, append-only шина событий, hook-миддлвары — для тех, кто любит покопаться |
| 🗂️ **Память** | SQLite FTS5 + поток событий сессии; трёхуровневая память, `/undo`, checkpoint, replay, экспорт Trajectory |
| 🧩 **Экосистема** | MCP (Model Context Protocol) + ACP (Agent Client Protocol) — втыкаешь свои инструменты и даёшься водить IDE |
| 🌍 **Десять языков** | По умолчанию упрощённый китайский, интерфейс любит подкидывать случайные локали, локализован и API |
| 🐍 **Чистый Python** | ~414 файлов `.py` / ~77k строк / 32 пакета / 15+ плагинов, MIT — грызи сколько влезет |

---

## Быстрые ответы

**В: Это правда «своя разработка»? Только без вранья.**
Ядро и почти все возможности (`kernel/`, `core/`, `acp/`, `tools/`, `ports/`) — на Python **от архитектуры до строчки кода написано с нуля**; с внешними протоколами просто сверяются интерфейсы. Один-единственный нюанс — терминальный TUI: та самая штука на `--tui` с её фирменным характером и палитрой **осознанно наследует стиль Kimi Code** (основной `#4FA8FF`, вращающийся moon-лоадер, двухстрочный статус-бар), потому что «хорошая механика — и не трогай». Это единственное, что сохранило его стиль; авторские права и атрибуция — в [NOTICE](NOTICE). Всё остальное — плоть и кровь Qingxiaotuan.

**В: Почему ты вечно суёшься перед выполнением команды?**
Фича, а не баг. Опасные команды по умолчанию требуют подтверждения; если YOLO попал в жёсткую красную линию — отказ в любом случае. Надоело — `qxt safe allow <cmd>` и не отключай охрану.

**В: Что умеет откатывать `/undo`?**
Все операции записи, что попали в журнал: один файл, один шаг, целый кусок. Под капотом — транзакционный журнал + `reverse_transform` из diff + снапшоты контрольных точек. Не панацея, но «ошибся = слил» превращается в «скорее всего вытащим обратно».

**В: А если он вычитает мои личные файлы?**
Политика разрешений зажимает инструменты в domain allow-list; исходящий сетевой трафик ограничен, чтобы ничего не вытекло; а те выводы, что уходят наружу, маскируют твои секреты. Безопасность для него не лозунг, а ежедневный KPI.

**В: Полный офлайн?**
`qxt models local` для поиска, `/offline` для управления Ollama. Без сети — тоже работает.

**В: Можно написать свои инструменты/плагины?**
Ещё бы. `@plugin` объявляет метаданные, в `activate(kernel)` регистрируешь/достаёшь сервисы через `kernel.provide(...)` / `kernel.require(...)`. Три шага:

```python
from ..core.kernel import Kernel, Plugin

@plugin("my.tool", provides=["loop_registry"])
class MyTool(Plugin):
    def activate(self, kernel):
        def _handler(ctx, query: str) -> str:
            return f"echo: {query}"
        kernel.provide("tools.my", _handler)
```

---

## Старт за пять минут

```bash
# 1) Ставим (комплект [dev] — всё при себе; нужен только API — бери [openai] [mcp])
git clone <этот репозиторий> && cd qingxiaotuan-agent-cli
python -m venv .venv && source .venv/bin/activate    # Windows: .venv\Scripts\activate
pip install -e ".[dev]"

# 2) Настраиваем модель (deepseek / groq-free / siliconflow-free / github-models / ollama … 14 встроенных профилей)
qxt setup
# или вручную: правим ~/.qingxiaotuan/config.yaml
#   model.provider: deepseek
#   model.model: deepseek-chat
#   model.api_key_env: DEEPSEEK_API_KEY      # ключи никогда не пишем открытым текстом

# 3) Погнали общаться
qxt
# или разово прощупать:
qxt --print run "Привет, представься в одном предложении"
```

---

## Лицо CLI: 25 под-команд + 36 слэш-команд

Самые ходовые из них:

| Команда | Зачем |
|---|---|
| `qxt` | Интерактивный TUI (скин Kimi Code) |
| `qxt setup` / `qxt models` | Настроить провайдера / список моделей (48 провайдеров) |
| `qxt agent` | Именованные агенты (совместимость с `.claude/agents` + трёхуровневый поиск) |
| `qxt acp` | Поднять ACP server, чтобы VS Code / Zed / JetBrains тебя погоняли |
| `qxt cron` | Фоновые задачи по расписанию |
| `qxt doctor` / `qxt bench` | Осмотр / бенчмарк |
| `qxt arch demo` | В один клик проверить, что пятислойный плагин на месте |

Самые ходовые слэш-команды: `/plan` (read-only) · `/model` (сменить модель) · `/undo`·`/impact` (откат + радиус поражения) · `/swarm` (мульти-агент) · `/log`·`/stats`·`/cost`·`/budget`·`/goal`·`/sandbox`·`/offline`·`/verify`·`/audit`·`/more`·`/help`. Полный список — `Ctrl-G` в TUI.

---

## Три главных цикла — одна и та же стабильность

- **ReActLoop**: подумал → сделал → посмотрел. Ритм по умолчанию.
- **PlannerExecuteLoop**: составил план → исполнил по шагам. Для длинных задач, где «одним прыжком» не выйдет.
- **DevLoop**: непрерывный контур разработчика — правит → тестирует → чинит.

Переключается на лету (например, `/mode plan`), не трогая остальное — одинаковый вход, одинаковые инструменты, одинаковый журнал безопасности.

---

## Устройство (если любишь читать схемы)

```
cli (commands) ──▶ kernel (микроядро + шина событий + contract)
                       ├── kernel/agent    (циклы ReAct/Planner/Dev, маршрутизация, сжатие, хуки)
                       ├── kernel/models   (роутер, каталог провайдеров, адаптеры)
                       ├── kernel/tools    (реестр, разрешения, диспетчеризация, MCP)
                       ├── kernel/mcp / acp (мосты на обе стороны)
                       └── kernel/session  (память / контекст / cron / аудит / skills)
```

Микроядро отдаёт реестр плагинов, контейнер сервисов и шину событий с изоляцией исключений. Движки — изолированные JSONL-подпроцессы, так что сбой одного не роняет главный цикл.

---

## Как это легально

- Несём снаружи и кусочек Kimi Code — не кант: единственное, что в стиль, это терминальный TUI (см. выше). Всё остальное — самостоятельная разработка, только сверка интерфейсов. Материалы стороннего происхождения атрибутируются в [NOTICE](NOTICE) по их лицензии (MIT).
- Мы держимся правила «заимствуй паттерны, а не копируй», с обязательным указанием источника (`qxt gh borrow`).

---

## Статус и известные ограничения

- Бета (`v0.2.014`). Фоновые запуски и потоковый путь Anthropic добиваются.
- Красные линии движка безопасности уже полной библиотеки его критических паттернов; косвенные вызовы (`sh -c`, `sudo -u`, подстановка команд) требуют рекурсивного раскрытия.
- Потоковое шифрование crypto-движка мигрирует к схеме с nonce.
- `commands.py` — крупный модуль, в планах — разбить.

---

## Лицензия

MIT — см. [LICENSE](LICENSE).

## Ссылки

- Репозиторий: https://github.com/Qingnai-Technology-kino-koki/Qingxiaotuan-Agent-CLI
- Issues: https://github.com/Qingnai-Technology-kino-koki/Qingxiaotuan-Agent-CLI/issues
- Changelog: [CHANGELOG.md](CHANGELOG.md)