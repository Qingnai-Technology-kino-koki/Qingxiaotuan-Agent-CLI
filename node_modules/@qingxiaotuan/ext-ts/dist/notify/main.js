"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * notify —— 青小团桌面通知引擎 (TS 侧)
 *
 * 跨平台发送桌面通知, 封装各系统原生能力:
 *   - Windows: PowerShell 的 BurntToast / 回退到 msg * (无需额外依赖)
 *   - macOS:   osascript display notification
 *   - Linux:   notify-send (libnotify)
 *
 * 也支持 "toast" 风格的轻量本地回退 (写入工作区 .notifications/ 供前端轮询)。
 *
 * IPC 方法:
 *   send   { title, message, level?, timeout_ms? } -> { ok, method }
 *   history{}                                      -> { items:[...] }
 *   clear {}                                       -> { ok }
 */
const protocol_ts_1 = require("../protocol.js");
const child_process_1 = require("child_process");
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
const os = __importStar(require("os"));
class Notifier {
    constructor() {
        this.history = [];
        const base = process.env.QXT_NOTIFY_DIR || path.join(os.tmpdir(), "qxt-notify");
        this.fallbackDir = base;
        this.historyFile = path.join(base, "history.json");
        try {
            fs.mkdirSync(base, { recursive: true });
            if (fs.existsSync(this.historyFile)) {
                this.history = JSON.parse(fs.readFileSync(this.historyFile, "utf-8"));
            }
        }
        catch { /* ignore */ }
    }
    persist() {
        try {
            fs.writeFileSync(this.historyFile, JSON.stringify(this.history.slice(-100)), "utf-8");
        }
        catch { /* ignore */ }
    }
    /** 检测当前平台可用的原生通知方式。 */
    detectMethod() {
        const platform = os.platform();
        if (platform === "win32") {
            // 优先尝试 BurntToast (PowerShell 模块), 否则用 msg
            try {
                const r = (0, child_process_1.spawn)("powershell", ["-NoProfile", "-Command", "Get-Command", "New-BurntToastNotification", "-ErrorAction", "SilentlyContinue"], { windowsHide: true });
                // 简单判定: 不抛错即可, 实际发送时再决定
                r.on("error", () => { });
                return "windows";
            }
            catch {
                return "windows";
            }
        }
        if (platform === "darwin")
            return "macos";
        return "linux";
    }
    send(title, message, level, timeout_ms = 5000) {
        const platform = os.platform();
        let method = "fallback";
        try {
            if (platform === "win32") {
                method = this.sendWindows(title, message, level);
            }
            else if (platform === "darwin") {
                method = this.sendMacos(title, message, level);
            }
            else {
                method = this.sendLinux(title, message, level);
            }
        }
        catch {
            method = "fallback";
        }
        if (method === "fallback")
            this.writeFallback(title, message, level);
        this.history.push({ ts: Date.now(), title, message, level });
        if (this.history.length > 100)
            this.history = this.history.slice(-100);
        this.persist();
        return { ok: true, method };
    }
    sendWindows(title, message, _level) {
        // 使用 PowerShell 直接调用 Shell.Application 的 Toast (无需 BurntToast 模块)
        const safe = (s) => s.replace(/"/g, "'").replace(/\n/g, " ");
        const ps = `
$ErrorActionPreference='SilentlyContinue'
[Windows.UI.Notifications.ToastNotificationManager, Windows.UI.Notifications, ContentType = WindowsRuntime] | Out-Null
$template=[Windows.UI.Notifications.ToastNotificationManager]::GetTemplateContent([Windows.UI.Notifications.ToastTemplateType]::ToastText02)
$texts=$template.GetElementsByTagName('text')
$texts.Item(0).AppendChild($template.CreateTextNode('${safe(title)}')) | Out-Null
$texts.Item(1).AppendChild($template.CreateTextNode('${safe(message)}')) | Out-Null
$notifier=[Windows.UI.Notifications.ToastNotificationManager]::CreateToastNotifier('Qingxiaotuan')
$notifier.Show($template)
`;
        const p = (0, child_process_1.spawn)("powershell", ["-NoProfile", "-Command", ps], { windowsHide: true, stdio: "ignore" });
        p.on("error", () => { });
        return "windows-toast";
    }
    sendMacos(title, message, _level) {
        const safe = (s) => s.replace(/"/g, '\\"').replace(/\\/g, "\\\\");
        const p = (0, child_process_1.spawn)("osascript", ["-e", `display notification "${safe(message)}" with title "${safe(title)}"`], { stdio: "ignore" });
        p.on("error", () => { });
        return "macos-osa";
    }
    sendLinux(title, message, level) {
        const icon = level === "error" ? "dialog-error" : level === "warning" ? "dialog-warning" : "dialog-information";
        const p = (0, child_process_1.spawn)("notify-send", ["-i", icon, title, message], { stdio: "ignore" });
        p.on("error", () => { });
        return "linux-notify-send";
    }
    writeFallback(title, message, _level) {
        try {
            const p = path.join(this.fallbackDir, `n_${Date.now()}.json`);
            fs.writeFileSync(p, JSON.stringify({ title, message, ts: Date.now() }), "utf-8");
        }
        catch { /* ignore */ }
    }
    getHistory() { return this.history; }
    clearHistory() { this.history = []; this.persist(); }
}
async function main() {
    const notifier = new Notifier();
    const server = new protocol_ts_1.IpcServer();
    server.register("send", (params) => {
        const title = String(params?.title || "青小团");
        const message = String(params?.message || "");
        const level = params?.level || "info";
        const timeout = params?.timeout_ms || 5000;
        if (!message)
            throw new Error("missing message");
        return notifier.send(title, message, level, timeout);
    });
    server.register("history", () => ({ items: notifier.getHistory() }));
    server.register("clear", () => { notifier.clearHistory(); return { ok: true }; });
    server.register("method", () => ({ method: notifier.detectMethod() }));
    await server.run();
}
main().catch((e) => { process.stderr.write(`notify fatal: ${String(e)}\n`); process.exit(1); });
//# sourceMappingURL=main.js.map