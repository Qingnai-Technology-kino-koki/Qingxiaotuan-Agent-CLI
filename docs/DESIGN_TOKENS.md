# 青小团 设计令牌规范（GUI / TUI）

> 用途：统一青小团 CLI 的交互式 TUI 与未来桌面 GUI 的设计语言——令牌体系、圆角尺度、字体分层、语义化命名。
> 品牌色保持青小团绿 `#16B78C`。

## 1. 设计令牌

### 品牌主色

| 令牌 | 值 | 说明 |
| --- | --- | --- |
| `--qx-primary` | `#16B78C` | 主品牌绿 |
| `--qx-primary-hover` | `#3DD6C4` | 主色 hover |
| `--qx-primary-active` | `rgba(22,183,140,1)` | 主色 active |

### 语义色板

| 语义 | 令牌 | 值 |
| --- | --- | --- |
| 危险 / 错误 | `--qx-danger` | `#ff3849` |
| 成功 | `--qx-success` | `#16c456` |
| 强调绿 | `--qx-accent-green` | `#32ff7d` |
| 警告 | `--qx-warning` | `#ffd230` |
| 橙 | `--qx-orange` | `#ff9500` |
| 紫 | `--qx-purple` | `#985ffb` |
| Diff 新增 | `--qx-diff-add` | `#2da44e` |

### 表面 / 边框（深色主题）

| 令牌 | 值 |
| --- | --- |
| `--qx-surface` | `#18181c` |
| `--qx-surface-raise` | `rgba(40,40,46,.85)` |
| `--qx-surface-row` | `rgba(255,255,255,.02)` |
| `--qx-border` | `rgba(255,255,255,.08)` |
| 中性灰阶 | `#FFFFFF` `#F7F7F7` `#F0F6FC` `#E6E6E6` `#1A1A1A` `#292929` `#CE9178` … |

### 圆角尺度（只用这几档，保持节奏一致）

`8px`（主导） › `6px` › `10px` › `4px` › `12px` › `50%`（胶囊 / 圆形） › `16px` › `3px` › `2px`

### 字体分层

- `--qx-font-base`：UI 正文（系统无衬线）
- `--qx-font-mono`：代码 / 日志（等宽栈 `ui-monospace, SFMono-Regular, Menlo, monospace`）
- 公式 / 编辑器字体：按需扩展

## 2. 设计纪律

1. **令牌化**：所有颜色 / 圆角集中为自定义属性，组件只引用令牌，便于换肤与暗色模式。
2. **语义化命名**：按用途命名（`--qx-danger` / `--qx-success`），而非按色值命名，降低误用。
3. **统一圆角尺度**：只用 4 / 6 / 8 / 10 / 12 / 16 + `50%` 几档。
4. **基础字体 + 等宽字体**二分：UI 用无衬线，代码 / 日志用等宽。
5. **状态三态**：主色、成功、危险各定义 hover / active 变体。

## 3. 落地

青小团主色 `#16B78C` / 强调 `#3DD6C4` 已实现于 `qingxiaotuan/ui/theme.py`。未来 GUI / TUI 主题统一沿用上表令牌，不再散落硬编码色值。

## 4. 说明

令牌化、语义色、统一圆角尺度、字体分层属行业通行的设计工程实践，青小团按其规范建立了自己的令牌表；品牌色保持青小团绿，不套用他方品牌色。
