# 青小团 版本策略 FAQ（语义化版本与 0.x 阶段说明）

> 本文面向**外部贡献者**，解释「功能已经很丰富，但版本号仍停留在 0.2」背后的语义化版本策略。
> 它是 [VERSION_POLICY.md](VERSION_POLICY.md)（强制规范）的**配套说明**：规范讲「规则」，本文讲「为什么」并回答最常问的问题。

---

## TL;DR

- 版本号停在 `0.2.x` **不代表功能少或不成熟**——它表达的是「**公共 API 与配置仍可能在不兼容的情况下变更**」。
- **版本号与功能数量无关**。项目已有约 40 家 Provider 适配、10 个内置能力引擎、完整的最小影响半径安全护栏与工具生态，但仍维持 0.x。
- 0.x 阶段的核心承诺：**每次升级都请重新阅读配置示例与变更说明**；破坏性变更会显式标注并给出迁移路径。

---

## 1. 为什么功能已经很丰富，版本号还是 0.2？

语义化版本（SemVer）规定：`0.y.z` 的 **`0` 主版本号表示「公共 API 尚未稳定」**。在 0.x 阶段：

- 即使只是一次主版本**内**的小升级，也**允许引入不兼容的 API / 配置变更**（只要变更在 Release Notes 中说明）；
- 一旦我们认为公共 API（CLI 接口、配置 schema、工具协议、插件契约）已经足够稳定、可以承诺向后兼容，就会发布 `1.0.0`，此后严格遵循 `MAJOR.MINOR.PATCH`。

换言之，`0.2` 是一条**诚实的声明**：「我们仍在主动演进 API 形态」，而不是「产品只做了 20%」。

---

## 2. 版本号与功能数量无关

- 我们**刻意不用**版本号高低来暗示「做了多少功能」。
- 判断是否值得升级的依据，是 [CHANGELOG.md](CHANGELOG.md) 与 Release Notes 里的**具体变更**，而不是数字大小。
- 因此你会看到两种「反直觉」情况：
  - 功能大幅增加的版本，版本号可能只从 `0.2.011` → `0.2.012`；
  - 一次重构配置 schema 的版本，可能直接 `0.2.012` → `0.3.0`（破坏性信号，须到 2027 春节后才会解锁）。

---

## 3. 什么时候发版？（发版时机判断标准）

一次发版（version bump）应满足以下**任一**条件，且建议由 CI 在合并后自动判定级别：

| 级别 | 触发条件 | 现有用法是否受影响 |
|------|----------|--------------------|
| **PATCH (z)** | 仅修复 bug、文档、内部重构，且**不改变任何外部可观察行为**（CLI 输出、配置键、工具参数） | 否 |
| **MINOR (y)** | 新增**向后兼容**的功能（新工具、新 Provider、带默认值的新配置键） | 否 |
| **MAJOR 进位（0.x 内）** | 引入**不兼容变更**——删除/重命名配置键、改变 CLI 子命令语义、修改工具协议、破坏插件契约 | **是** |

> 在 0.x 阶段，我们用 `0.y` 的进位来表达「破坏性」，等价于一次 MAJOR 信号（例如 `0.2.z` → `0.3.0`）。

发版前**必须**由 CI 跑通全量测试与 `CHANGELOG` 校验；未通过不发布。

---

## 4. 破坏性变更怎么处理？

- **必须**在 [CHANGELOG.md](CHANGELOG.md) 的 `Breaking Changes` 段显式列出，并附**迁移步骤**（旧键 → 新键、旧命令 → 新命令）。
- 配置加载层应提供**有限期的兼容垫片**（deprecation shim）：旧键仍可读、打印弃用警告，并在下一个 MINOR 移除。
- 工具协议变更需同步更新 `ToolContext` 契约与所有内置引擎，避免「部分更新」导致运行期 `AttributeError`（CI 中有契约测试兜底）。
- 升级后运行 `qxt config validate` 可做类型 / 枚举 / Provider 一致性校验，提前暴露破坏性配置差异。

---

## 5. 作为贡献者，我需要注意什么？

- 改了任何「别人可能依赖」的东西（CLI 参数、配置键、工具签名、插件接口），请同步更新文档与 CHANGELOG，并按第 3 节选择 bump 级别。
- **不确定是否破坏性？** 默认按「破坏性」处理，并在 PR 说明，由维护者裁决。
- 必读优先级：**SECURITY.md → VERSION_POLICY.md → 本 FAQ → CHANGELOG.md**。

---

## 6. 相关链接

- [VERSION_POLICY.md](VERSION_POLICY.md) — 强制版本迭代规范
- [CHANGELOG.md](CHANGELOG.md) — 变更记录（含 Breaking Changes 段）
- [README.md](README.md) — 项目总览与快速开始
- [SUPPORTED_MODELS.md](SUPPORTED_MODELS.md) — 各 Provider / 模型的实测兼容性与验证方式
